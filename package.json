{
  "name": "autocomplete-weexvue",
  "version": "0.1.6",
  "description": "weex tag name autocompletions",
  "main": "./lib/main",
  "license": "MIT",
  "repository": " ",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    }
  },
  "dependencies": {
  },

  "scripts": {
	"install": "node-gyp rebuild"
  }
}
